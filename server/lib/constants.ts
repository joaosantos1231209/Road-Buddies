// Re-export app constants from client for consistent usage in server code
import { APP_NAME, APP_DESCRIPTION, APP_VERSION } from '../../client/src/lib/constants';

export { APP_NAME, APP_DESCRIPTION, APP_VERSION };